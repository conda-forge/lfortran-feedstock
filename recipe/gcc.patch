diff --git a/src/bin/lfortran.cpp b/src/bin/lfortran.cpp
index 3024bd480..5d8a0c0fd 100644
--- a/src/bin/lfortran.cpp
+++ b/src/bin/lfortran.cpp
@@ -844,7 +844,7 @@ int compile_to_object_file_cpp(const std::string &infile,
                 out.open(outfile_empty);
                 out << " ";
             }
-            std::string cmd = "gcc -c " + outfile_empty + " -o " + outfile;
+            std::string cmd = "cc -c " + outfile_empty + " -o " + outfile;
             int err = system(cmd.c_str());
             if (err) {
                 std::cout << "The command '" + cmd + "' failed." << std::endl;
@@ -979,7 +979,7 @@ int link_executable(const std::vector<std::string> &infiles,
             if (platform == LFortran::Platform::macOS) {
                 CC = "clang";
             } else {
-                CC = "gcc";
+                CC = "cc";
             }
             std::string base_path = "\"" + runtime_library_dir + "\"";
             std::string options;
